# Database 설정 (Docker 환경)
# /app/data는 docker-compose volumes에서 ../data:/app/data 로 마운트됨
DATABASE_TYPE=SQLITE
SQLITE_PATH=/app/data/db/pulseone.db

# Secondary Database (Optional)
SECONDARY_DATABASE_ENABLED=false
