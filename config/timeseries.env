# =============================================================================
# 시계열 데이터베이스 설정 (timeseries.env)
# =============================================================================
INFLUX_HOST=localhost
INFLUX_PORT=8086
INFLUX_ORG=pulseone
INFLUX_BUCKET=telemetry_dev
INFLUX_TOKEN_FILE=${SECRETS_DIR}/influx.key
INFLUX_SSL=false

# 배치 설정
INFLUX_BATCH_SIZE=1000
INFLUX_FLUSH_INTERVAL_SEC=10
INFLUX_RETENTION_POLICY=30d