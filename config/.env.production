# PulseOne Windows Production Environment
# Node.js 전용 설정만 여기에 — 연결 설정은 각 .env 파일이 SSOT
# (database.env, redis.env, timeseries.env 등은 CONFIG_FILES로 로드됨)
NODE_ENV=production
AUTO_INITIALIZE_ON_START=true
START_COLLECTOR_ON_BOOT=false
BACKEND_PORT=3000
# Windows 패키지 전용: 인증 없이 API 접근 허용
ALLOW_NO_AUTH=true
# InfluxDB/INFLUX 토큰 (secrets 폴더 없으므로 직접 설정)
INFLUX_TOKEN=pulseone-influx-token-windows-2026
INFLUXDB_TOKEN=pulseone-influx-token-windows-2026
# JWT 인증 (ALLOW_NO_AUTH=false 전환 시 아래 값을 강력한 랜덤 문자열로 교체)
JWT_SECRET=pulseone-jwt-secret-change-me-before-enabling-auth
