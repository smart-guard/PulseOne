# =============================================================================
# PulseOne .gitignore
# 데이터베이스, 로그, 설정 파일 등을 git에서 제외
# =============================================================================


# =============================================================================
# 데이터베이스 파일들 (중요!)
# =============================================================================
# SQLite 데이터베이스
*.db
*.sqlite
*.sqlite3
data/db/
data/backup/
data/temp/

# 데이터베이스 백업 및 덤프
*.sql.backup
*_backup_*.db
*.dump

# 데이터베이스 로그
*.db-journal
*.db-wal
*.db-shm

# Node.js
node_modules/
npm-debug.log*

# Python
**__pycache__**/
*.py[cod]
.venv/

# 환경변수 및 민감 정보
.env
.env.local
.env.*.local
!config/.env.template
config/.env.dev
config/.env.staging  
config/.env.prod
secret.json
credentials.json

# API Keys 보안 (추가)
secrets/
*.key
.anthropic/
config/secrets/*.key
config/secrets/

# 로그 및 시스템 파일
*.log
.DS_Store

# 테스트/빌드 아티팩트
coverage/
dist/
build/
test-results/
.vscode/
*.o
bin/
structure.txt

# Docker 관련 (추가)
.env.override
docker-compose.override.yml

# 압축 파일 (개발용)
*.tar.gz
*.zip.vscode/settings.json
secrets/
*.key
